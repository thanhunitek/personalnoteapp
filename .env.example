# Upstash Redis / Vercel KV credentials
# Get these from your Vercel project settings after linking a KV store
# or from your Upstash console

KV_REST_API_URL=
KV_REST_API_TOKEN=

# PIN Protection
# Generate a SHA-256 hash of your PIN:
#   echo -n "YOUR_PIN" | shasum -a 256
# Then copy the hash value here (without the trailing " -")
APP_PIN_HASH=
